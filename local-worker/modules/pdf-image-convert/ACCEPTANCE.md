# ACCEPTANCE.md - local-worker-pdf-image-convert

## 验收状态

`PASSED`

## 验收清单

- [x] 模块目标已实现 — PDF/图片互转双向支持，单文件处理，预留批量扩展。
- [x] 不包含禁止内容 — 无密钥、模型大文件、缓存、构建产物。
- [x] 测试记录已写入 PROGRESS.md — 60 项单元测试全部通过。
- [x] 新依赖已登记 — PyMuPDF 1.27.2.3、Pillow 12.2.0、pytest 9.1.1 待写入全局台账。
- [x] 代码关键逻辑有中文注释 — 所有公开方法、核心处理逻辑均有中文注释。

## 功能验证

| 功能 | 状态 | 说明 |
|------|------|------|
| PDF → 图片（文件路径） | ✅ | 支持多页 PDF 逐页渲染 |
| PDF → 图片（bytes） | ✅ | 支持内存中 PDF 数据 |
| PDF → 图片（指定 DPI） | ✅ | 72-600 DPI 可调 |
| PDF → 图片（页码范围） | ✅ | 支持部分页面选择 |
| PDF → 图片（JPEG 输出） | ✅ | 支持 PNG/JPEG 两种格式 |
| PDF → 图片（保存到目录） | ✅ | 自动创建输出目录 |
| 图片 → PDF（文件路径列表） | ✅ | 多张图片合并为一个 PDF |
| 图片 → PDF（bytes 列表） | ✅ | 支持内存中图片数据 |
| 图片 → PDF（PIL Image 列表） | ✅ | 支持 PIL Image 直接输入 |
| 图片 → PDF（RGBA 处理） | ✅ | 透明通道自动合成白色背景 |
| 图片 → PDF（保存到文件） | ✅ | 自动创建父目录 |
| 参数校验 | ✅ | DPI、JPEG质量、页码范围、页数限制 |
| 错误处理 | ✅ | 文件不存在、格式不支持、空数据等 |
| PDF 信息查询 | ✅ | 页数、元数据等 |
| 支持格式查询 | ✅ | 输入输出格式清单 |

## 是否允许合并

是。模块开发完成，60 项测试全部通过，等待更新全局依赖台账后提交推送。
